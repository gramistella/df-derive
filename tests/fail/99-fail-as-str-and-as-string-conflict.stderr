error: field `name` has both `as_str` and `as_string`; pick one — `as_str` borrows via `AsRef<str>` (no allocation), `as_string` formats via `Display` (allocates per row)
 --> tests/fail/99-fail-as-str-and-as-string-conflict.rs:7:5
  |
7 | /     #[df_derive(as_str)]
8 | |     #[df_derive(as_string)]
9 | |     name: String,
  | |________________^
